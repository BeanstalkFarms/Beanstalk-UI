import { createTheme } from '@material-ui/core/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3B3B3B',
    },
    secondary: {
      main: '#DCBA6A',
    },
    background: {
      main: '#EFF7FF',
    },
  },
});
export default theme;
