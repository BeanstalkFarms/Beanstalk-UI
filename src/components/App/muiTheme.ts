import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#3B3B3B',
    },
    secondary: {
      main: '#DCBA6A',
    },
    background: {
      main: '#EFF7FF',
    },
  },
});
export default theme;
