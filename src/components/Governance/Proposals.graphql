#import "./Proposal.fragment.graphql"
query Proposals($space_in: [String!]!) {
  proposals (
    where: {
      space_in: $space_in
    },
    orderBy: "created",
    orderDirection: desc
  ) {
    ...Proposal
  }
}
