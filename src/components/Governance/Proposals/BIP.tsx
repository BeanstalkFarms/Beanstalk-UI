import React from 'react';
import { Stack, Typography } from '@mui/material';

const BIP : React.FC<{ proposals: any; }> = (props) => (
  <Stack>
    <Typography>TEST</Typography>
  </Stack>
  );

export default BIP;
