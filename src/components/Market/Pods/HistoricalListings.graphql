query HistoricalListings(
  $historyIDs: [String!]!
) {
  podListings(where: {
    historyID_in: $historyIDs
  }, orderBy: updatedAt, orderDirection: desc) {
    id
    status
    # ---
    amount
    pricePerPod
    index
    originalIndex
    historyID
    # ---
    updatedAt
  }
}