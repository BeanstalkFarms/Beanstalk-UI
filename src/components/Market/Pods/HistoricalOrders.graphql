query HistoricalOrders(
  $historyIDs: [String!]!
) {
  podOrders(where: {
    historyID_in: $historyIDs
  }, orderBy: updatedAt, orderDirection: desc) {
    id
    historyID
    status
    podAmount
    podAmountFilled
    pricePerPod
    maxPlaceInLine
    updatedAt
  }
}