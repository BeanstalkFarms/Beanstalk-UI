query SeasonalPrice($season_lte: Int, $first: Int) {
  seasons(
    where: { seasonInt_lte: $season_lte },
    first: $first,
    orderBy: seasonInt,
    orderDirection: desc,
  ) {
    id
    seasonInt
    timestamp
    twap
  }
}
