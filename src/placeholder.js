// Without a .js file somewhere in the src/ directory,
// the create-react-app development server show every 
// Typescript error. I couldn't find a way to fix this
// with configuration (even using CRA's own env vars for this),
// so for now we'll keep this placeholder file.
const placeholder = '';
export default placeholder;
